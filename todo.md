# Todo

[x] Design and implement responsive layout behavior so the app fits 13-inch laptops without losing the sleek worksheet look (deferred)
[x] Sync README with the actual project structure and formulas
[x] Add Tailwind width support for `max-w-8xl` or replace it with a built-in size
- [x] Add a lint script and basic code-quality checks
[x] Resolve the README wording mismatch around Monte Carlo scope
- [x] Add or expand tests for any behavior changed by the fixes
- [x] Add CSV export for the current stack rows from the top toolbar
- [ ] Add JSON export for the full report payload later
- [x] Add generated PDF export for the full report with official-document styling
- [x] Fix PDF header and section spacing overlap in the report export
- [x] Add CSV import for stack rows alongside export
- [ ] Add yield / pass-rate estimation from Monte Carlo
- [ ] Add tolerance sensitivity analysis for dominant contributors
- [ ] Add stack comparison between two saved variants
- [ ] Add spec limit checks with go / no-go status
- [ ] Add a tolerance what-if slider for quick scenario testing
- [ ] Add warning highlights when one row dominates the stack
- [ ] Add units switching with consistent conversion
